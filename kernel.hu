#ifndef KERNEL_H_
#define KERNEL_H_

typedef struct _Image {
    unsigned width;
    unsigned height;
    unsigned data[1]; 
} Image;


__global__ void stencilCUDA(Image* in, Image* out);

#endif /* KERNEL_H_ */
